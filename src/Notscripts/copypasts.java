package Notscripts;

//
//if (inventory.contains("Pot"));
  //  		walking.webWalk(banks);
    // 			if (Banks.LUMBRIDGE_UPPER.contains(myPlayer()));
     //				bank.open();
     //					if (getBank().isOpen());
    // 						bank.depositAll();
     //						
     //						
    						
    						
    						
    						
    						// While loop for cutting wood
    		 //				if (inventory.contain("Tinder Box")) {
    		 //					int inventoryspcae = 2;
    		 //					while (inventoryspace = 2;) {
    		 //						//Cut wood + sleep+not animaiting
    		 //						inventoryspace++;
    		 //						}
    								 //				}else {
    			 //			if (inveotry.contain("Axes")) {		
    		 //				int inventoryspcae = 1;
    		 //					while (inventoryspace = 1;) {
    								 //					//Cut wood + sleep+not animaiting
    			 //					inventoryspace++;
    			 //					}
    			 //				}